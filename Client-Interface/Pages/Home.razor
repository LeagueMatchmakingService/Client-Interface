@page "/"
@using System.Text.Json
@inject IJSRuntime JSRuntime
@inject ServerAppDemo.Models.MatchCreation mc
<div class="hero-area">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="left-content">
                    <div class="content">
                        <h5 class="subtitle">
                            Matchmaking Service
                        </h5>
                        <h1 class="title">
                            for League of Legends
                        </h1>
                        @*<p class="text">
                                Play, and rise your MMR to reach the top of the leaderboard
                            </p>*@
                        <div class="links">
                            <NavLink href="Matchmaking" class="mybtn1">PLAY NOW</NavLink>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



@code {

    private string _divObjective = string.Empty;
    private string _divPlatform = string.Empty;
    private string _divVersion = string.Empty;


    protected override async void OnAfterRender(bool firstRender)
    {
        base.OnAfterRender(firstRender);
        if (firstRender)
        {
        }
    }

}